// security_config.h — generated by CMake from security_config.h.in
#pragma once

// Exact allowed package name (baked in)
#define BAKED_EXPECTED_PKG "@EXPECTED_PKG@"

// Allowed signer SHA-256 fingerprints (lowercase hex, no colons).
static const char* const BAKED_ALLOWED_CERTS[] = {
@_LINES@
};
static const int BAKED_ALLOWED_CERTS_COUNT =
    sizeof(BAKED_ALLOWED_CERTS) / sizeof(BAKED_ALLOWED_CERTS[0]);
